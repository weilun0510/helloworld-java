# ==================== 测试环境配置 ====================
# 此配置文件会在 spring.profiles.active=test 时生效

# ==================== 数据源配置（测试环境）====================
# 测试数据库连接 URL（建议使用独立的测试数据库）
spring.datasource.url=jdbc:mysql://8.130.142.163:3306/mybatis?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
# 数据库用户名
spring.datasource.username=mybatis
# 数据库密码
spring.datasource.password=aKAndbJFJxtFZb2W

# ==================== MyBatis-Plus 配置（测试环境）====================
# SQL 日志输出（测试环境启用，方便调试）
mybatis-plus.configuration.log-impl=org.apache.ibatis.logging.stdout.StdOutImpl

# ==================== 测试环境特定配置 ====================
# 关闭热部署（测试环境不需要）
spring.devtools.restart.enabled=false

# ==================== Knife4j 配置（测试环境）====================
# 启用 Knife4j 增强文档（测试环境启用）
knife4j.enable=true

# ==================== SpringDoc OpenAPI 配置（测试环境）====================
# 启用 API 文档（测试环境启用）
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true

# ==================== 日志配置（测试环境）====================
# 测试环境使用 INFO 级别日志
logging.level.root=INFO
logging.level.org.example.helloworld=INFO
logging.level.org.springframework.web=INFO

